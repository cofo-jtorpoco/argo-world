# Go build artifacts
/**/app
*.test
# local env overrides
*.local.env

# compiled service binaries (built into images, never committed)
/src/*/scoreboard
/src/*/match-feed
/src/*/match-watcher

# local port-forward logs
.pf-logs/
